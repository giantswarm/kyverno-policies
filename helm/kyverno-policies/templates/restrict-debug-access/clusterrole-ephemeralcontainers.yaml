{{- $disallowEphemeral := (hasKey .Values.debugAccessPolicies.policies "disallow-ephemeral-containers") and (get .Values.debugAccessPolicies.policies "disallow-ephemeral-containers").enabled -}}
{{- $disallowUnapproved := (hasKey .Values.debugAccessPolicies.policies "disallow-unapproved-debug-access") and (get .Values.debugAccessPolicies.policies "disallow-unapproved-debug-access").enabled -}}
{{- if or $disallowEphemeral $disallowUnapproved }}
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: kyverno:admission-controller:disallow-unapproved-debug-access:ephemeralcontainers
  labels:
    rbac.kyverno.io/aggregate-to-admission-controller: "true"
rules:
  - apiGroups:
      - ""
    resources:
      - pods/ephemeralcontainers
    verbs:
      - get
      - list
      - watch
{{- end }}
