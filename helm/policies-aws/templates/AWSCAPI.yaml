# THIS FILE IS GENERATED WITH 'make generate' - DO NOT EDIT MANUALLY
apiVersion: kyverno.io/v1
kind: ClusterPolicy
metadata:
  name: aws-capi-policy
spec:
  background: false
  rules:
  # aws-label mutates all CAPA CRs to have the watch-label filter if their Cluster has version 20.0.0.
  - name: aws-label
    match:
      resources:
        kinds:
        - infrastructure.cluster.x-k8s.io/v1alpha3/AWSCluster
        - infrastructure.cluster.x-k8s.io/v1alpha3/AWSClusterControllerIdentity
        - infrastructure.cluster.x-k8s.io/v1alpha3/AWSClusterRoleIdentity
        - infrastructure.cluster.x-k8s.io/v1alpha3/AWSClusterStaticIdentity
        - infrastructure.cluster.x-k8s.io/v1alpha3/AWSMachine
        - infrastructure.cluster.x-k8s.io/v1alpha3/AWSMachinePool
        - infrastructure.cluster.x-k8s.io/v1alpha3/AWSMachineTemplate
        - infrastructure.cluster.x-k8s.io/v1alpha3/AWSManagedMachinePool
        - controlplane.cluster.x-k8s.io/v1alpha3/AWSManagedControlPlane
        - bootstrap.cluster.x-k8s.io/v1alpha3/EKSConfigTemplate
    exclude:
      resources:
        selector:
          matchLabels:
            cluster.x-k8s.io/watch-filter: capi
    context:
    - name: releaseVersion
      apiCall:
        urlPath: "/apis/cluster.x-k8s.io/v1alpha3/namespaces/{{ `{{` }} request.object.metadata.namespace {{ `}}` }}/clusters"
        jmesPath: "items[?metadata.name == '{{ `{{` }}request.object.metadata.labels.\"cluster.x-k8s.io/cluster-name\"{{ `}}` }}'].metadata.labels.\"release.giantswarm.io/version\" | [0]"
    preconditions:
      any:
      - key: "{{ `{{` }} releaseVersion {{ `}}` }}"
        operator: Equals
        value: "20.0.0"
    mutate:
      patchStrategicMerge:
        metadata:
          labels:
            +(cluster.x-k8s.io/watch-filter): "capi"
            cluster.x-k8s.io/watch-filter: "capi"

  # awscluster-default adds default values to fields in a AWSCluster CR if it has the watch-filter label and the values are completely missing.
  - name: awscluster-default
    match:
      resources:
        kinds:
        - infrastructure.cluster.x-k8s.io/v1alpha3/AWSCluster
        selector:
          matchLabels:
            cluster.x-k8s.io/watch-filter: capi
    mutate:
      patchStrategicMerge:
        spec:
          # Create values if missing
          +(region): {{ .Values.region }}
          +(sshKeyName): {{ .Values.sshKeyName }}

  # awscluster-default-empty-string adds default values to fields in a AWSCluster CR if it has the watch-filter label and the values are empty strings.
  - name: awscluster-default-empty-string
    match:
      resources:
        kinds:
        - infrastructure.cluster.x-k8s.io/v1alpha3/AWSCluster
        selector:
          matchLabels:
            cluster.x-k8s.io/watch-filter: capi
    mutate:
      patchStrategicMerge:
        spec:
          # Replace empty strings
          region: |-
            {{ `{{` }} regex_replace_all('^$', '{{ `{{` }}@{{ `}}` }}', '{{ .Values.region }}') {{ `}}` }}
          sshKeyName: |-
            {{ `{{` }} regex_replace_all('^$', '{{ `{{` }}@{{ `}}` }}', '{{ .Values.sshKeyName }}') {{ `}}` }}

  # awscluster-cni adds default values to CNI ingress rules.
  - name: awscluster-cni
    match:
      resources:
        kinds:
        - infrastructure.cluster.x-k8s.io/v1alpha3/AWSCluster
        selector:
          matchLabels:
            cluster.x-k8s.io/watch-filter: capi
    mutate:
      overlay:
        spec:
          # always create (and override) cni rules
          networkSpec:
            cni:
              cniIngressRules:
                - description: allow cni traffic across nodes and control plane
                  protocol: "-1"
                  fromPort: -1
                  toPort: -1

  # awsmanagedcontrolplane-default adds default values to fields in a AWSManagedControlPlane CR if it has the watch-filter label and the values are completely missing.
  - name: awsmanagedcontrolplane-default
    match:
      resources:
        kinds:
        - controlplane.cluster.x-k8s.io/v1alpha3/AWSManagedControlPlane
        selector:
          matchLabels:
            cluster.x-k8s.io/watch-filter: capi
    mutate:
      patchStrategicMerge:
        spec:
          # Create values if missing
          +(region): {{ .Values.region }}
          +(sshKeyName): {{ .Values.sshKeyName }}

  # awsmanagedcontrolplane-default-empty-string adds default values to fields in a AWSManagedControlPlane CR if it has the watch-filter label and the values are empty strings.
  - name: awsmanagedcontrolplane-default-empty-string
    match:
      resources:
        kinds:
        - controlplane.cluster.x-k8s.io/v1alpha3/AWSManagedControlPlane
        selector:
          matchLabels:
            cluster.x-k8s.io/watch-filter: capi
    mutate:
      patchStrategicMerge:
        spec:
          # Replace empty strings
          region: |-
            {{ `{{` }} regex_replace_all('^$', '{{ `{{` }}@{{ `}}` }}', '{{ .Values.region }}') {{ `}}` }}
          sshKeyName: |-
            {{ `{{` }} regex_replace_all('^$', '{{ `{{` }}@{{ `}}` }}', '{{ .Values.sshKeyName }}') {{ `}}` }}

  # awsclusterroleidentity-default adds default values to fields in a AWSClusterRoleIdentity CR if it has the watch-filter label and the values are completely missing.
  - name: awsclusterroleidentity-default
    match:
      resources:
        kinds:
        - infrastructure.cluster.x-k8s.io/v1alpha3/AWSClusterRoleIdentity
        selector:
          matchLabels:
            cluster.x-k8s.io/watch-filter: capi
    mutate:
      patchesJson6902: |-
        - op: add
          path: "/spec/roleARN"
          value: {{ .Values.awsClusterRoleIdentity.roleARN }}
        - op: add
          path: "/spec/sourceIdentityRef/name"
          value: {{ .Values.awsClusterRoleIdentity.sourceIdentityRef.name }}
        - op: add
          path: "/spec/sourceIdentityRef/kind"
          value: {{ .Values.awsClusterRoleIdentity.sourceIdentityRef.kind }}

  # awsclusterroleidentity-default-empty-string adds default values to fields in a AWSClusterRoleIdentity CR if it has the watch-filter label and the values are empty strings.
  - name: awsclusterroleidentity-default-empty-string
    match:
      resources:
        kinds:
        - infrastructure.cluster.x-k8s.io/v1alpha3/AWSClusterRoleIdentity
        selector:
          matchLabels:
            cluster.x-k8s.io/watch-filter: capi
    mutate:
      patchStrategicMerge:
        spec:
          # Replace empty strings
          roleARN: |-
            {{ `{{` }} regex_replace_all('^$', '{{ `{{` }}@{{ `}}` }}', '{{ .Values.awsClusterRoleIdentity.roleARN}}') {{ `}}` }}
          sourceIdentityRef:
            name: |-
              {{ `{{` }} regex_replace_all('^$', '{{ `{{` }}@{{ `}}` }}', '{{ .Values.awsClusterRoleIdentity.sourceIdentityRef.name}}') {{ `}}` }}
            kind: |-
              {{ `{{` }} regex_replace_all('^$', '{{ `{{` }}@{{ `}}` }}', '{{ .Values.awsClusterRoleIdentity.sourceIdentityRef.kind }}') {{ `}}` }}

  # awsmachinetemplate-default adds default values to fields in a AWSMachineTemplate CR if it has the watch-filter label and the values are completely missing.
  - name: awsmachinetemplate-default
    match:
      resources:
        kinds:
        - infrastructure.cluster.x-k8s.io/v1alpha3/AWSMachineTemplate
        selector:
          matchLabels:
            cluster.x-k8s.io/watch-filter: capi
    mutate:
      patchesJson6902: |-
        - op: add
          path: "/spec/template/spec/instanceType"
          value: {{ .Values.awsControlPlane.instanceType }}

  # awsmachinetemplate-default-empty-string adds default values to fields in a AWSMachineTemplate CR if it has the watch-filter label and the values are empty strings.
  # its used to default instanceType for a control plane machine in case its empty
  - name: awsmachinetemplate-default-empty-string
    match:
      resources:
        kinds:
        - infrastructure.cluster.x-k8s.io/v1alpha3/AWSMachineTemplate
        selector:
          matchLabels:
            cluster.x-k8s.io/watch-filter: capi
    mutate:
      patchStrategicMerge:
        spec:
          template:
            spec:
              instanceType: |-
                {{ `{{` }} regex_replace_all('^$', '{{ `{{` }}@{{ `}}` }}', '{{ .Values.awsControlPlane.instanceType }}') {{ `}}` }}

  # awsmachinetemplate-disk set default values for disks in a AWSMachineTemplate CR if it has the watch-filter label and control-plane role filter.
  - name: awsmachinetemplate-disks
    match:
      resources:
        kinds:
        - infrastructure.cluster.x-k8s.io/v1alpha3/AWSMachineTemplate
        selector:
          matchLabels:
            cluster.x-k8s.io/watch-filter: capi
            cluster.x-k8s.io/role: control-plane
    mutate:
      patchStrategicMerge:
        spec:
          template:
            spec:
              rootVolume:
                size: {{ .Values.awsMachineTemplate.rootVolume.size }}
                type: {{ .Values.awsMachineTemplate.rootVolume.type }}
              nonRootVolumes:
              - deviceName: {{ .Values.awsMachineTemplate.nonRootVolumes.etcd.name }}
                size: {{ .Values.awsMachineTemplate.nonRootVolumes.etcd.size }}
                type: {{ .Values.awsMachineTemplate.nonRootVolumes.etcd.type }}
                encrypted: {{ .Values.awsMachineTemplate.nonRootVolumes.etcd.encrypted }}
              - deviceName: {{ .Values.awsMachineTemplate.nonRootVolumes.varlibdocker.name }}
                size: {{ .Values.awsMachineTemplate.nonRootVolumes.varlibdocker.size }}
                type: {{ .Values.awsMachineTemplate.nonRootVolumes.varlibdocker.type }}
                encrypted: {{ .Values.awsMachineTemplate.nonRootVolumes.varlibdocker.encrypted }}
              - deviceName: {{ .Values.awsMachineTemplate.nonRootVolumes.varlibkubelet.name }}
                size: {{ .Values.awsMachineTemplate.nonRootVolumes.varlibkubelet.size }}
                type: {{ .Values.awsMachineTemplate.nonRootVolumes.varlibkubelet.type }}
                encrypted: {{ .Values.awsMachineTemplate.nonRootVolumes.varlibkubelet.encrypted }}

  # awsmachinepool-default default missing values on AWSMachinePool CR with capi watch filter
  - name: awsmachinepool-default
    match:
      resources:
        kinds:
        - infrastructure.cluster.x-k8s.io/v1alpha3/AWSMachinePool
        selector:
          matchLabels:
            cluster.x-k8s.io/watch-filter: capi
    mutate:
      patchStrategicMerge:
        spec:
          awsLaunchTemplate:
            +(rootVolume):
              +(size): {{ .Values.awsMachinePool.rootVolume.size }}
              +(type): {{ .Values.awsMachinePool.rootVolume.type }}
