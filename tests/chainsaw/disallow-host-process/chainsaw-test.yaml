apiVersion: chainsaw.kyverno.io/v1alpha1
kind: Test
metadata:
  name: disallow-host-process
spec:
  steps:
  - name: create a bad pod with hostProcess
    try:
    - create:
        expect:
        - check:
            ($error != null): true
        file: bad-pod.yaml
  - name: create a good pod without hostProcess
    try:
    - create:
        expect:
        - check:
            ($error != null): false
        file: good-pod.yaml
